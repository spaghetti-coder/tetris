---
services:
  tetris:
    image: ghcr.io/spaghetti-coder/tetris:master
    ports:
      - "8080:8080"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - .:/app
    restart: unless-stopped
